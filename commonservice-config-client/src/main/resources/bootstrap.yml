eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8882
spring:
  application:
    name: commonservice-config-client
  cloud:
    config:
      discovery:
        service-id: commonservice-config-server
        enabled: true
      label: master
      profile: dev
      enabled: true
      fail-fast: true
